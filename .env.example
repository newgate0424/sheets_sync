# ============================================
# PRODUCTION DEPLOYMENT CHECKLIST
# ============================================
# 1. Copy this file to .env
# 2. Fill in ALL required values
# 3. Never commit .env to git
# 4. Upload .env to production server
# ============================================

# MongoDB Configuration (REQUIRED)
# Get from MongoDB Atlas: https://cloud.mongodb.com
# Add retryWrites=true&w=majority&tlsAllowInvalidCertificates=true for production SSL issues
MONGODB_URI="mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority&tlsAllowInvalidCertificates=true"
DATABASE_USER_URL="mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority&tlsAllowInvalidCertificates=true"

# Database Configuration (REQUIRED - Choose ONE)
# Option 1: MySQL (Recommended)
DATABASE_URL="mysql://user:password@host:3306/database"

# Option 2: PostgreSQL
# DATABASE_URL="postgresql://user:password@host:5432/database"

# MySQL Specific (if using MySQL)
DB_HOST="localhost"
DB_PORT=3306
DB_USER="root"
DB_PASSWORD="your_password"
DB_NAME="your_database"

# Cron Job Tokens (REQUIRED for scheduled sync)
CRON_SYNC_TOKEN="your_random_secure_token_min_32_chars"
NEXT_PUBLIC_CRON_TOKEN="your_public_token_min_16_chars"

# Admin User (for scripts)
ADMIN_PASSWORD="your_strong_admin_password"

# Google Sheets API (REQUIRED)
# Place your credentials.json file in the root directory
# Get from: https://console.cloud.google.com/apis/credentials

# ============================================
# PRODUCTION SERVER SETUP STEPS
# ============================================
# 1. Upload .env file to: /var/www/vhosts/ads169th.com/httpdocs/.env
# 2. Ensure Node.js version >= 18.x: node --version
# 3. Test MongoDB connection: node -e "require('mongodb').MongoClient.connect(process.env.MONGODB_URI)"
# 4. Test DATABASE_URL: check MySQL/PostgreSQL is running
# 5. Restart application: pm2 restart all (or your process manager)
# ============================================
